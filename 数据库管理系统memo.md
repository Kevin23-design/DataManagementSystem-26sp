# 数据库初始化位置

我的数据库初始化在(F:\CodeData\MongoDB)

# 启动数据库

打开cmd，输入下述命令启动数据库

```bash
mongod --dbpath="E:\Software_Data\MongoDB\data\testdb"
```

在打开另一个cmd，输入下述命令，启动mongosh，并连接mongod服务。

```bash
mongosh
```